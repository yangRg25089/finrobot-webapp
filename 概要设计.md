# FinRobot WebApp 概要设计文档

<div align="center">
  <a href="概要设计.md">中文</a> | <a href="概要设计_ja.md">日本語</a>
</div>

## 1. 项目概述

### 1.1 项目简介
FinRobot WebApp 是一个基于 FinRobot 框架的全栈金融分析应用，提供可视化的量化交易策略运行平台。该应用集成了多种 AI 代理，支持年度报告分析、股票预测、RAG 问答等功能，为用户提供一站式的金融数据分析服务。

### 1.2 技术栈
- **后端**: FastAPI + FinRobot + Python 3.10
- **前端**: React 18 + TypeScript + Vite + Tailwind CSS
- **AI 框架**: FinRobot (基于 AutoGen)
- **数据存储**: 文件系统 + ChromaDB (向量数据库)
- **国际化**: react-i18next (支持中文、英文、日文)

### 1.3 核心特性
- 🚀 实时流式脚本执行
- 📊 多维度金融数据分析
- 🤖 智能 AI 代理集成
- 📁 文件生成与预览
- 🌍 多语言支持
- 📈 历史记录管理
- 🔄 参数化脚本配置

## 2. 系统架构

### 2.1 整体架构图
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端 (React)   │    │   后端 (FastAPI) │    │   AI 代理层     │
│                 │    │                 │    │                 │
│ • ScriptForm    │◄──►│ • main.py       │◄──►│ • FinRobot      │
│ • ResultViewer  │    │ • script_manager│    │ • AutoGen       │
│ • LogsPanel     │    │ • history_manager│   │ • 各种 AI 代理   │
│ • Sidebar       │    │ • utils         │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   浏览器存储     │    │   文件系统       │    │   外部 API      │
│                 │    │                 │    │                 │
│ • localStorage  │    │ • static/       │    │ • OpenAI API    │
│ • 用户偏好       │    │ • history/      │    │ • SEC API       │
│ • 脚本选择       │    │ • output/       │    │ • 金融数据 API   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 2.2 核心模块

#### 2.2.1 前端模块
- **App.tsx**: 主应用组件，管理全局状态
- **ScriptForm.tsx**: 脚本参数配置表单
- **ResultViewer.tsx**: 结果展示与历史记录管理
- **LogsPanel.tsx**: 实时日志显示
- **Sidebar.tsx**: 脚本选择侧边栏
- **Topbar.tsx**: 顶部导航栏

#### 2.2.2 后端模块
- **main.py**: FastAPI 应用入口，API 路由定义
- **script_manager.py**: 脚本执行管理器，支持流式输出
- **history_manager.py**: 对话历史管理
- **utils.py**: 通用工具函数集合

#### 2.2.3 AI 代理模块
- **tutorials_wrapper/**: 封装的金融分析脚本
  - **beginner/**: 入门级脚本
  - **advanced/**: 高级脚本

## 3. 功能模块设计

### 3.1 脚本执行系统

#### 3.1.1 脚本管理
```python
# 脚本统一接口
def run(params: dict, lang: str) -> dict:
    return {
        "result": messages,  # 对话结果
        "generated_files": files_info,  # 生成的文件
        "metrics": performance_data  # 性能指标
    }
```

#### 3.1.2 流式执行
- 使用 Server-Sent Events (SSE) 实现实时输出
- 支持 stdout/stderr 分离
- 错误处理与异常捕获
- 执行状态实时更新

#### 3.1.3 参数解析
- 自动解析脚本参数定义
- 支持多种数据类型：string, boolean, date, array
- 动态表单生成
- 参数验证与默认值处理

### 3.2 结果展示系统

#### 3.2.1 对话历史
- Markdown 格式渲染
- 消息折叠/展开
- 角色标识显示
- 时间戳记录

#### 3.2.2 文件管理
- 自动文件收集
- 图片预览功能
- PDF 文档预览
- 文件下载支持

#### 3.2.3 历史记录
- 按脚本分类存储
- 时间戳目录结构
- 历史记录查询
- 记录删除功能

### 3.3 国际化系统

#### 3.3.1 支持语言
- 中文 (zh)
- 英文 (en)  
- 日文 (ja)

#### 3.3.2 翻译内容
- 界面文本
- 脚本描述
- 参数标签
- 错误消息

### 3.4 配置管理

#### 3.4.1 API 配置
- OpenAI API 配置
- SEC API 配置
- 其他金融数据 API

#### 3.4.2 模型配置
- 支持多种 AI 模型
- 动态模型选择
- 参数配置管理

## 4. 数据流设计

### 4.1 脚本执行流程
```
用户选择脚本 → 配置参数 → 提交执行 → 流式输出 → 结果展示 → 历史保存
     ↓              ↓           ↓          ↓          ↓          ↓
  ScriptForm → ScriptManager → SSE → LogsPanel → ResultViewer → HistoryManager
```

### 4.2 文件存储结构
```
backend/
├── static/
│   ├── output/           # 脚本输出文件
│   │   └── {script_name}/
│   │       └── {date}/
│   │           └── {timestamp}/
│   └── history/          # 对话历史
│       └── {script_name}/
│           └── {date}/
│               └── {timestamp}/
└── tutorials_wrapper/    # 脚本源码
    ├── beginner/
    └── advanced/
```

### 4.3 API 接口设计

#### 4.3.1 核心接口
- `GET /api/tutorial-scripts`: 获取可用脚本列表
- `GET /api/run-script/stream`: 流式执行脚本
- `GET /api/models`: 获取可用模型列表
- `GET /api/history/{script_name}`: 获取脚本历史
- `DELETE /api/history/{script_name}`: 删除历史记录

#### 4.3.2 数据格式
```typescript
// 脚本信息
interface ScriptInfo {
  script_name: string;
  folder: string;
  params: Record<string, ParamConfig>;
}

// 参数配置
interface ParamConfig {
  type: 'string' | 'boolean' | 'date' | 'string[]' | 'number';
  defaultValue: any;
}

// 执行结果
interface ExecutionResult {
  result: Message[];
  generated_files: FileInfo[];
  error?: string;
}
```

## 5. 技术实现细节

### 5.1 前端技术栈

#### 5.1.1 状态管理
- React Hooks 进行状态管理
- 自定义 Hook 封装业务逻辑
- 本地存储持久化用户偏好

#### 5.1.2 实时通信
- EventSource API 接收 SSE 流
- 自动重连机制
- 错误处理与状态同步

#### 5.1.3 组件设计
- 函数式组件 + TypeScript
- 响应式布局设计
- 无障碍访问支持

### 5.2 后端技术栈

#### 5.2.1 API 框架
- FastAPI 提供高性能 API
- 自动 API 文档生成
- 类型安全的数据验证

#### 5.2.2 异步处理
- asyncio 支持并发处理
- 线程池执行 CPU 密集型任务
- 流式响应优化用户体验

#### 5.2.3 文件管理
- Pathlib 进行路径操作
- 自动目录创建与清理
- 文件权限管理

### 5.3 AI 集成

#### 5.3.1 FinRobot 集成
- 统一的代理接口
- 多模型支持
- 工具函数集成

#### 5.3.2 对话管理
- 消息标准化处理
- 多语言指令支持
- 历史上下文管理

## 6. 部署与运维

### 6.1 开发环境
```bash
# 后端启动
cd backend
conda activate finrobot
uvicorn main:app --reload --port 8000

# 前端启动
cd frontend
npm install
npm run dev
```

### 6.2 生产环境
- Docker 容器化部署
- Nginx 反向代理
- 环境变量配置管理
- 日志监控与告警




FinRobot WebApp 是一个功能完整、架构清晰的金融分析平台，通过现代化的技术栈和模块化的设计，为用户提供了强大的 AI 驱动的金融分析能力。系统具有良好的扩展性和维护性，能够满足不同层次的用户需求，并为未来的功能扩展奠定了坚实的基础。

---
